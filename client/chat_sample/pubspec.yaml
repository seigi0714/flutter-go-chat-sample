name: we_portal_sample
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_riverpod: ^0.14.0+3
  freezed_annotation:
  
dev_dependencies:
  build_runner:
  freezed:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true